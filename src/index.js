import React from "react";
import ReactDOM from "react-dom";

const root = document.getElementById("root");

ReactDOM.render(
  <h1 className="text-sky-300 font-bold text-4xl">I'am Litte D</h1>,
  root
);
